import me.gmx.process.nodes.LabelKey;
import me.gmx.process.nodes.LabelNode;
import org.junit.jupiter.api.Test;

import java.util.UUID;

public class StructureTest {

    @Test
    public void uuidTest(){
        /*UUID uuid = UUID.randomUUID();
        String uuidString = Long.toHexString(uuid.getMostSignificantBits())
                + Long.toHexString(uuid.getLeastSignificantBits());
        UUID keyuuid = LabelKey.hashUUID(uuid, 0);*/

        /*LabelNode node = new LabelNode("a");
        LabelKey key = new LabelKey(node);
        System.out.printf("Node UUID: %s \n", node.getId().toString());
        System.out.printf("Key UUID: %s \n", key.getId().toString());
        System.out.printf("String: %s \n", uuidString);*/
    }

}
